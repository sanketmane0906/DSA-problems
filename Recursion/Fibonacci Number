
In this problem we have to return the fibonacci number

Fibonacci number is sum of last 2 numbers 

example n=2;

f(2)=f(0)+fib(1)=0+1=1

but here base condition is must 
if(n==0) return 0;: This condition checks if the input n is 0. If it is, the function returns 0. 
This is because the 0th Fibonacci number is 0.

if(n==1) return 1;: This condition checks if the input n is 1. If it is, the function returns 1. 
This is because the 1st Fibonacci number is 1.

So lets code it:-

class Solution
{
    public int fib(int n)
    {
        if(n==0) return 0;
        if(n==1) return 1;

        return fib(n-1)+fib(n-2);
    }
}
